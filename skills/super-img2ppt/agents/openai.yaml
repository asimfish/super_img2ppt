interface:
  display_name: "Super Img2PPT"
  short_description: "将图片重建为可编辑 PPTX 与 SVG，校验字体、排版和重叠"
  default_prompt: "Use $super-img2ppt to reconstruct this image as editable PPTX and SVG, then inspect actual rendered output for font and overlap errors."
