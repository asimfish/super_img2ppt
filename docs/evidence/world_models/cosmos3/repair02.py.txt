import json
p='/tmp/cosmos3-xNo4Kd/job/scene.json';s=json.load(open(p))
for e in s['slides'][0]['elements']:
 if e['id'].startswith('in') and e['id'].endswith('tilde'): e['box'][1]+=3;e['box'][3]=15
 if e['id']=='f2sub2':e['box'][0]+=5
 if e['id']=='matrix_h':e.update(allow_overlap_with=['matrix_v'],overlap_reason='Source attention quadrant divider lines intersect at center')
open(p,'w').write(json.dumps(s,indent=2))
