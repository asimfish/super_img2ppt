The first isolated-import assertion used an unresolved /var temporary path against Path.resolve(), which returns /private/var on macOS. The printed imported module was already inside the unpacked package at version 0.3.6. The harness was corrected to resolve both paths before comparison; a fresh unpacked run passed composition, actual export and both ROI cases. No runtime change was needed. See commands.json and result.json for the final fresh run.
