import json,pathlib,hashlib
p=pathlib.Path(__file__).parent;s=2894/2048
o=json.loads((p/'published_job/pages/page_001/ocr.json').read_text())
r=[{'id':f'text_{i:02}','kind':'text','box':[max(0,round(x['box'][0]-4)),max(0,round(x['box'][1]-4)),round(x['box'][2]+8),round(x['box'][3]+8)],'ocr':x['text']} for i,x in enumerate(o['lines']) if i not in [6,14,17,18,19,20,21,22,23,24,33,34,39,40,43]]
for i,b in enumerate([[281,94,47,17],[468,167,14,49],[1280,90,59,16],[1620,90,69,16],[1090,318,16,35],[1182,318,15,35],[282,3,842,9],[658,113,8,216]]):r.append({'id':f'connector_{i}','kind':'geometry','box':[round(v*s) for v in b]})
(p/'roi_contract.json').write_text(json.dumps({'frozen_before_authoring':True,'source':'published.png','coordinate_system':'source pixels','ink':'RGB max < 170 for text and connector isolated ROIs; source and actual PPTX render resized to source with Lanczos','thresholds':{'max_ink_edge_delta_px':4,'ink_iou_min':0.70},'rois':r,'topology':['Image to global encoder','Image bypass to grounding encoder','Global encoder to region encoder','Global and region encoders join image-region prompts','Image-region prompts to V-L to LLM to L-P to output prompts','Selected output prompts to pixel decoder','Grounding encoder to pixel decoder to mask image','Instruction to LLM; LLM to grounded conversation','All four lower downstream panels preserved'],'required_text':'All readable text editable including prompt captions, all downstream responses and phrase highlights','raster_boundary':'Photos and robot artwork only; simple geometric token stacks native'},indent=2))
print(len(r))
