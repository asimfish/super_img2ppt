# Semantic-redraw correction — 2026-09-13

The user's correction distinguished artificial-looking drawing construction from minimalism.
The skill now routes the former to an opt-in, agent-authored candidate scene, with the existing
check/build runtime, rather than promising that style-field patches solve raster defects.
No new automatic redraw CLI is claimed.

Independent forward evaluation inspected two user-supplied figures locally. It selected native
icon reconstruction, formula/typography review, matrix-geometry review and local spacing while
preserving information density, colors with meaning, conceptual curves and connectivity. A
faithful-only request correctly excluded unsolicited refinement. Evaluation exposed ambiguous
coordinate-preservation wording; SKILL.md now explicitly scopes it to faithful conversion.

Local pilot replaces seven cropped illustrations with independently editable outline groups,
retypesets selected prose and adjusts affected label spacing. It retains mathematical text,
matrix data and existing curve coordinates. This is a first candidate, not proof of uniformly
better aesthetics. The private manuscript, figure crops, candidate and source-derived change
records stay in local ignored output directories and are not included in this repository.

Existing automatic geometry/font/render checks still apply. Dense mathematical glyph-width
diagnostics can remain review findings; no PowerPoint/WPS UI or AI-origin detection is claimed.
