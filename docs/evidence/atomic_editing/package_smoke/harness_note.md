Initial smoke fixture defined native edit groups but omitted layout containers for its label/formula over a filled module body. Preflight correctly rejected four unintended overlaps: edit groups do not grant containment or collision exemptions. Corrected only the authored fixture by declaring the actual encoder_body container; retried from a fresh unpacked package. The original failing scene/report are preserved. No runtime or threshold change.
The second run passed actual build and native/group assertions but the archive step attempted to create the already-existing evidence directory. Fixed archive bookkeeping with exist_ok=True, then reran from another fresh unpacked package; no conversion output directory was reused.
